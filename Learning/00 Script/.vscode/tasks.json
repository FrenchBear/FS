{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run F# interactive on current file",
            "type": "shell",
            "command": "dotnet fsi --load:\"${file}\"",
            "problemMatcher": []
        }
    ]
}